config {
  type: "operations",
  tags: ["staging"],
  hasOutput: true,
  dependencies: ["external_tables"]
}

DECLARE query STRING;

SET query = (
  SELECT CONCAT(
    'CREATE OR REPLACE VIEW ${self()} AS SELECT ',
    STRING_AGG(
      CONCAT(
        '`', column_name, '` AS ',
        REGEXP_REPLACE(
          REGEXP_REPLACE(
            REGEXP_REPLACE(
              LOWER(TRIM(column_name)),
              r'[^a-z0-9]+', '_'
            ),
            r'_+', '_'
          ),
          r'^_|_$', ''
        )
      )
    ),
    ' FROM `leep-data-system.paint.industry_full_raw`'
  )
  FROM `leep-data-system.paint.INFORMATION_SCHEMA.COLUMNS`
  WHERE table_name = 'industry_full_raw'
);

EXECUTE IMMEDIATE query;
